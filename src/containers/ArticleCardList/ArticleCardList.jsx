import React from 'react';
import ArticleCard from '../../components/ArticleCard/ArticleCard';

const ArticleCardList = (articleInfo) => {
    const {articleInfo} = props;

  return (
    <div>ArticleCardList</div>
  )
}

export default ArticleCardList